## flow-control Entrance Scripts

Rules to follow:

1. Don't forget to update `flow_control/cli.py` to add an entry point for this script if needed.
2. We prefer to put all args in Pydantic typed toml files instead of CLI args.