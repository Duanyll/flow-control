# Trackio frontend

Enhanced frontend for Trackio, provides a better "Media" view to group and filter media items by training step.

```bash
TRACKIO_DIR=$(pwd)/runs/.trackio uv run trackio show --frontend frontend --host 0.0.0.0
```