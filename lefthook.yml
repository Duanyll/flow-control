pre-commit:
  parallel: false
  commands:
    ruff-format:
      run: uv run ruff format .
      stage_fixed: true

    ruff-check:
      run: uv run ruff check --fix .
      stage_fixed: true

    # pyright:
    #   run: uvx pyright